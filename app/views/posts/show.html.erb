<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<!-- <p>
  <strong>User:</strong>
  <%= @post.user_id %>
</p>
 -->
<%if @post.user_id == current_user.id%>
 <%= link_to 'Edit', edit_post_path(@post) %> | <%end%> 
<%= link_to 'Back', posts_path %> |<% pre_like = @post.likes.find { |like| like.user_id == current_user.id} %>

<%#=@post.likes.count%>
<% if pre_like %>
  <%= link_to 'Unlike', post_like_path(@post, pre_like), method: :delete %>(<%=@post.likes.count%>)
<% else %>
  <%= link_to 'Like', post_likes_path(@post), method: :post %>(<%=@post.likes.count%>)
<% end %>

<h2>Comments</h2>
    <div class="comments-container">
    <%= render 'comments/comments' %>
    </div>
       <h2>Add a comment:</h2>
      <%#if  Comment.where(:blog_id => @blog.id, user_id: current_user.id).present?
       %>
     <%= render 'comments/form'%>
     <%#end %>

    <%= link_to 'Back', posts_path %>


